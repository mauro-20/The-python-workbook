# Area of a room

width = float(input('type the width of the room in meters\n'))
length = float(input('type the length of the room in meters\n'))
area = width * length
print('the area of the room is %.2f squared meters' % area)
